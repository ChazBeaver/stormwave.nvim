# stormwave.nvim

`stormwave.nvim` is the Neovim colorscheme for the broader **Stormwave** theme ecosystem.

This repo contains the Neovim theme itself. It is intended to pair with the Omarchy theme repo:

- **Omarchy theme repo:** `omarchy-stormwave-theme`
- **Neovim theme repo:** `stormwave.nvim`

## What this repo is

This repo is focused on Neovim only.

It provides the editor colorscheme, highlight groups, and plugin styling needed for Neovim to match the rest of the Stormwave theme.

## Relationship to Omarchy

`stormwave.nvim` is the editor-side counterpart to `omarchy-stormwave-theme`.

That means:

- `stormwave.nvim` handles Neovim colors and highlights
- `omarchy-stormwave-theme` handles desktop and app theming for Omarchy
- both are meant to share the same palette and visual direction

## Theme pairing

Use these together for a matching setup:

- **Neovim:** `stormwave.nvim`
- **Omarchy:** `omarchy-stormwave-theme`

## Goal

The goal of `stormwave.nvim` is to make Neovim feel like a natural part of the wider Stormwave environment instead of a separate, mismatched editor theme.
